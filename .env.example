# API base URL. Leave unset in local dev — requests go to /catalog/* and Vite
# proxies them to http://localhost:8080 (avoids CORS during development).
#
# Set this for production builds when the API is on another host, e.g.:
# VITE_API_BASE_URL=https://api.example.com
